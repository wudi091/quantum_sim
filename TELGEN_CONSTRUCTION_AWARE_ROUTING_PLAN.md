# 基于 TELGEN 的可泛化构造感知量子路由方案

## 文档定位

本文记录当前确认的研究方案，用于指导后续建模、验证和实验设计。

- 当前已实现教师与在线验证原型：构造 DAG 的名义时隙展开、开始时隙枚举、两阶段连续线性规划、IPM 原始变量轨迹记录、静态教师数据生成与负载校准、与 LP 使用相同变量和约束的小规模两阶段 0/1 MILP 审计器、硬约束解码器，以及周期微批次到达下的持久 SeQUeNCe 滚动执行；GNN 尚未实现。
- 当前研究只保留本方案，不并行维护其他学习或优化方案。
- 本方案以 TELGEN 的求解轨迹监督学习思想为方法基础，但研究问题、决策对象、约束体系和物理验证均面向量子网络重新设计。
- Q-DDCA 和 Q-CAST 保留为路径层基线。
- SeQUeNCe 是唯一物理仿真后端，规划层不重复实现量子物理过程。

## 一、研究问题

研究集中式量子网络中按固定周期成批到达的端到端纠缠请求。传统量子路由通常只为每个请求选择一条路径，然后沿固定流程执行纠缠生成和交换。

这一抽象忽略了一个关键事实：同一条路径并不唯一对应一种纠缠构造方式。对于一条给定路径，仍然可以采用不同的：

- 基础链路纠缠生成批次；
- 纠缠交换依赖关系；
- 交换执行顺序；
- 可并行交换组合；
- 中间纠缠段保留与释放方式。

这些构造差异会改变中间纠缠段的生命周期、量子内存占用、资源冲突、请求完成时间以及整个批次的完成情况。因此，本研究不再把构造过程视为路径确定后的固定执行细节，而是把它提升为路由决策的一部分。

最终问题定义为：

> 给定周期到达的量子纠缠请求，在有限量子内存和物理资源约束下，滚动联合选择每个待处理请求的路径与构造计划，使批次执行在时间维度上更加高效，从而提高完成吞吐并降低请求完成时间。

## 二、研究范围

当前主问题限定为周期微批次滚动场景：

- 每隔固定时隙产生固定数量的新请求，不采用泊松到达；
- 每轮只规划当前已到达且尚未完成、尚未超时的请求；
- 网络拓扑和物理配置在一个 episode 内保持不变；
- 每个请求需要建立端到端纠缠；
- 规划层联合决定路径和构造计划；
- 已启动计划保持不变，未来资源占用作为后续决策的预留；
- 未接纳请求与物理失败但未超时的请求可以进入后续轮次；
- SeQUeNCe 根据规划结果推进真实物理过程；
- 评价指标以 episode 完成吞吐和请求完成时间为主，同时检查保真度、成功率和资源约束。

静态同时到达场景仍用于生成和校准 LP 教师样本，但它是训练数据的一个
子场景，不再是在线主评测协议。

当前不把以下问题纳入主线：

- 泊松或任意随机连续到达；
- 无界路径生成；
- 会重排已启动计划的复杂在线修复；
- 完整随机控制策略；
- 在线随机控制与策略学习。

这些内容可以作为后续扩展，但不应干扰第一篇论文对核心问题的验证。

## 三、决策对象

每个请求的决策由两部分组成。

第一部分是路径，即请求经过哪些量子节点和物理链路。

第二部分是构造计划，用来描述在该路径上如何建立端到端纠缠，主要包含：

- 哪些基础链路纠缠可以同时生成；
- 哪些交换操作相互依赖；
- 哪些交换可以并行执行；
- 交换依赖如何形成完整的构造结构；
- 中间纠缠段何时被占用、消费和释放。

规划器最终需要为每个请求选择一个完整的“路径与构造计划”组合，而不是先独立选路径，再使用统一的固定构造程序。

## 四、优化目标与约束

主要优化目标包括：

1. 提高固定调度窗口内完成的请求数量，体现网络的批次服务能力。
2. 降低请求平均完成时间，体现请求级服务速度。

必要时可以进一步考察尾部完成时间、公平性和服务成功率，但不能让过多次要目标模糊论文的主结论。

规划模型至少需要表达以下约束：

- 每个被接纳的请求只能选择一个路径与构造计划组合；
- 链路、交换节点和量子内存在同一时间范围内不能被超额使用；
- 交换操作必须满足其前置纠缠段依赖；
- 同一个纠缠资源不能被多个操作重复消费；
- 构造结果必须形成请求源端到目的端的有效纠缠；
- 计划必须满足保真度和有效期要求；
- 批次内不同请求之间的资源竞争必须被统一考虑。

## 五、规划层与物理层隔离

系统保持严格的单向调用关系。

规划层负责：

- 表示请求、候选路径和候选构造计划；
- 建立批次级资源与时序抽象；
- 联合选择路径和构造计划；
- 输出可执行的离散规划结果。

物理层由 SeQUeNCe 完全负责：

- 基础链路纠缠生成；
- 量子信道和探测过程；
- 量子内存占用与释放；
- 纠缠交换；
- 概率性成功与失败；
- 保真度变化；
- 退相干与过期；
- 真实物理时间推进。

规划层只能通过中性接口提交计划并读取执行结果，不能直接操作 SeQUeNCe 内部对象，也不能自行模拟或修改物理状态。

需要特别区分两条时间线：

- 教师求解器的迭代过程表示优化算法逐步寻找方案的过程；
- SeQUeNCe 的事件时间表示量子网络真正执行方案的物理过程。

二者相互独立，不能把教师的一轮迭代解释为一个物理时隙或一次量子事件。

## 六、基于 TELGEN 的总体方法

本方案采用 TELGEN 的核心思想：不只学习优化器的最终答案，而是学习优化器从初始状态逐步接近高质量解的完整求解轨迹。

整体过程分为五个阶段：

1. 将批量请求的路径与构造计划联合选择描述为一个带目标和约束的名义规划问题。
2. 使用线性规划内点法教师求解该问题，并记录各轮中间状态和最终结果。
3. 将规划问题表示为由决策、约束和目标共同组成的图结构。
4. 训练图神经网络模仿教师的迭代求解过程，使其学习决策之间的关系、资源约束传播以及目标改进方向。
5. 将模型输出转换为满足硬约束的离散路径与构造计划，再交给 SeQUeNCe 进行物理执行和评价。

图神经网络的目的不是预测某条链路是否“看起来更好”，而是近似求解整个批次级联合规划问题。训练后，希望它能在未见拓扑、未见请求分布和更大网络规模上快速给出接近教师质量的方案。

## 七、教师模型

主教师只使用一个线性规划内点法求解器，与 TELGEN 的核心路线保持一致。

教师提供的监督信息包括：

- 各轮迭代中的规划变量状态；
- 目标值的变化过程；
- 约束满足程度的变化过程；
- 最终的连续规划结果。

教师产生的连续值表示对候选方案的选择倾向或资源分配趋势，不表示物理上可以建立分数形式的纠缠。最终方案必须经过受约束的离散化，得到每个请求实际采用的路径和构造计划。

小规模精确搜索可以用于检查教师与离散化结果的质量，但只作为验证工具，不作为第二个教师。SeQUeNCe 负责物理执行和评价，同样不属于教师模型。

## 八、物理一致性门槛

本方案最重要的前置验证不是图神经网络训练，而是确认名义规划模型与真实物理执行之间具有足够的一致性。

必须优先检查：教师认为更优的路径与构造计划，在 SeQUeNCe 中经过多次随机物理执行后，是否通常也具有更高的完成吞吐或更低的完成时间。

如果教师对候选方案的排序与 SeQUeNCe 结果严重不一致，那么图神经网络即使准确模仿教师，也只会快速产生错误决策。因此：

- 排序一致性是进入大规模数据生成之前的必要门槛；
- 需要在多类拓扑、负载和物理参数下进行检查；
- 若一致性不足，应先修正规划抽象、目标或约束，而不是直接增加模型复杂度。

## 九、论文创新点

论文不能仅表述为“把 TELGEN 应用于量子网络”。预期贡献应形成以下完整链条：

1. 提出构造感知量子路由问题，将纠缠构造方式从执行细节提升为路由决策变量。
2. 建立能够同时描述路径选择、交换依赖、中间纠缠生命周期、有限内存和批次资源竞争的联合规划模型。
3. 设计适合路径与构造联合决策的图表示，使模型能够理解请求、候选方案、物理资源、目标和约束之间的关系。
4. 利用教师的完整求解轨迹训练可泛化规划器，而不是只模仿最终标签。
5. 证明模型能够泛化到未见拓扑和更大网络，并显著降低规划时间。
6. 使用 SeQUeNCe 独立验证规划结果的真实物理吞吐、完成时间、成功率和保真度。

其中，问题定义和量子构造建模是区别于 TELGEN 的核心创新；求解轨迹学习是实现可泛化快速规划的方法创新。

## 十、实验验证框架

实验应围绕四个问题展开。

### 1. 构造感知是否必要

比较联合选择方法与只选择路径、固定构造方式的方法，验证同一路径上的构造选择是否能够改善批次吞吐和请求完成时间。

### 2. 学习求解器是否有效

比较图神经网络与教师求解器的方案质量、可行率和规划时间，确认模型是否能够以更低代价获得接近教师的结果。

### 3. 是否具有泛化能力

在训练期间未见的拓扑、请求规模、负载强度和更大网络上测试，不能只报告同分布场景结果。

### 4. 名义优化是否转化为物理收益

所有主要结论最终都必须由 SeQUeNCe 结果支持，不能只使用教师目标值或规划层估计值作为论文性能结论。

主要对比对象包括：

- 只进行路径选择的方法；
- 固定构造计划的方法；
- Q-DDCA 和 Q-CAST；
- 强启发式联合规划方法；
- 原始教师求解器；
- 只学习最终解、不学习求解轨迹的方法；
- 完整的构造感知图神经网络方法。

主要评价指标包括：

- 完成请求数量和完成吞吐；
- 平均完成时间和尾部完成时间；
- 物理成功率和保真度达标率；
- 方案可行率；
- 与教师或小规模精确结果的质量差距；
- 规划时间；
- 跨拓扑和跨规模泛化性能；
- 名义规划排序与物理执行排序的一致性。

## 十一、主要风险

### 风险一：教师与物理现实不一致

名义规划模型可能低估随机失败、退相干或中间纠缠等待造成的影响。这是当前最严重的风险，必须通过早期一致性实验处理。

### 风险二：连续结果难以转换为离散方案

教师可能产生质量较高但难以直接执行的连续结果。如果离散化损失过大，需要调整问题表示或训练目标，而不能把损失隐藏在后处理阶段。

### 风险三：候选集合覆盖不足

如果候选路径或构造计划过少，模型上限会被候选空间限制；如果候选过多，教师求解和训练数据生成的成本会迅速增加。需要在覆盖率和规模之间取得可解释的平衡。

### 风险四：退化为 TELGEN 的直接应用

如果论文只更换网络类型而没有提出新的构造决策、约束表示和物理验证机制，贡献不足。必须始终围绕“路径与构造计划联合决策”组织方法和实验。

### 风险五：退化为普通可泛化路由

如果删除构造计划，只研究跨拓扑路径选择，虽然问题更容易，但会失去本方案最有价值的研究动机，也难以解释为什么需要新的量子路由框架。

## 十二、研究推进顺序

后续研究应按照以下顺序推进：

1. 固定周期微批次主问题及其静态教师子问题、主要目标和约束边界。
2. 明确定义有限且有代表性的路径与构造计划候选空间。
3. 建立名义规划教师，并验证其基本可行性。
4. 使用 SeQUeNCe 检查教师排序与物理结果的一致性。
5. 只有一致性达到可接受水平后，才生成大规模求解轨迹数据。
6. 训练并验证图神经网络对教师轨迹的学习能力。
7. 完成跨拓扑、跨负载和跨规模泛化实验。
8. 通过消融实验确认构造感知、轨迹监督和约束表示各自的贡献。

## 十三、最终方案概括

> 本研究面向周期到达的批量纠缠请求，联合选择请求路径和纠缠构造计划；以线性规划内点法作为单一教师，按照 TELGEN 的方式学习完整求解轨迹，得到能够跨拓扑和跨规模泛化的图神经网络规划器；最终离散计划只通过中性接口提交给 SeQUeNCe，由 SeQUeNCe 独立完成全部物理过程和性能验证。

该方案的论文定位是面向批量纠缠请求的可泛化构造感知量子路由。

## 十四、周期微批次滚动协议

本研究不使用泊松到达。每隔固定的 N 个时隙产生固定的 m 个新请求。
默认取 N=4、m=10，总请求数为 100，因此到达时隙为
0、4、8、……、36，每个到达点产生 10 个请求。每个请求的 TTL 为 16，
episode 自动排空到时隙 52。

控制器只在周期边界决策。每轮规划集合包含本轮新请求、此前未接纳的积压
请求，以及物理失败但仍在 TTL 内的请求；未来请求不可见。新计划的开始
时隙必须属于当前周期 `[t, t+N)`，但完整构造允许跨越下一决策边界，最晚
完成时间由请求自己的 TTL 与 episode 终点共同限制。已经运行的计划保持
不变，其未来资源占用作为后续规划的预留。

默认主实验通过不同随机种子生成不同 Waxman 拓扑与请求端点。Q-CAST
作为统一滚动环境下的适配基线，使用完全相同的 `EpisodeSpec`、到达队列、
决策周期、路径候选数、TTL、SeQUeNCe 后端和评价指标；它固定采用
left-deep 构造，并且不调用 TELGEN 的 LP 教师或硬约束解码器。
