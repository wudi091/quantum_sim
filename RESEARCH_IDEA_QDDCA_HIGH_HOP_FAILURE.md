# 研究 Idea：Q-DDCA 的高跳局部观测失效

记录日期：2026-07-13

## 暂定题目

**When Local Congestion Misleads: High-Hop Failure of Decentralized Quantum Routing**

中文：**当局部拥塞产生误导：去中心化量子路由的高跳失效**

## 一句话核心故事

> Q-DDCA 在短路径中使用局部拥塞近似路径质量；随着跳数增加，局部观测与端到端可完成性逐渐脱钩，使其拥塞规避策略在高跳场景中产生额外性能损失。

## 论文定位

- 类型：New Problem/Setting Paper，后续可发展为 Technique Paper。
- 当前贡献重点：定义、复现并解释“高跳局部观测失效”。
- 方法贡献只有在失效机制被验证后才能确定，不能预先指定为 GNN、RL 或机会路由。

## 研究背景

Q-DDCA 面向大规模量子网络，通过邻居内存、局部拥塞和历史接受情况进行去中心化下一跳选择。它本身支持多跳路由，但每次决策主要依据局部状态。端到端路径变长后，请求结果还受到远端资源竞争、剩余跳数、连续交换成功率、等待时间和退相干的共同影响。

## 核心限制

1. **局部代表性衰减**：邻居当前可用内存或拥塞程度不一定能代表后续路径的真实风险。
2. **状态混叠**：两个全局网络状态可能向当前节点提供相同的 Q-DDCA 局部观测，但需要相反的最优下一跳动作。
3. **高跳额外损失未被隔离**：所有量子路由都会受到物理交换成功率随跳数下降的影响，因此必须证明 Q-DDCA 还存在由局部决策造成的额外损失。

## 研究目标

> 在排除实现错误和不可避免的物理衰减后，确定 Q-DDCA 的局部观测是否在高跳网络中产生可重复的决策错误，并刻画局部拥塞与端到端可完成性脱钩的条件。

## 关键挑战

1. 将 Q-DDCA 的实现 bug 与算法机制失效区分开。
2. 将不可避免的高跳物理衰减与局部观测导致的额外损失区分开。
3. 构造局部观测相同、全局状态不同、最优动作相反的成对状态。

## 方法模块

### Module A：可信高跳复现

修复 hop-budget/reroute 语义，使用真实的纠缠生成、交换失败、有限内存和退相干模型，在相同资源预算下扫描真实源宿跳数。

### Module B：决策级失败归因

记录每个决策时刻的局部观测、候选下一跳、后续路径状态、最终结果和 oracle 动作，区分物理失败与策略选择错误。

### Module C：局部状态混叠诊断

建立匹配状态对：保持 Q-DDCA 可观察变量相同，只改变远端资源和可靠性；检验 Q-DDCA 是否作出相同动作而 oracle 动作相反。

## 最小可证伪实验

1. 固定当前节点、目标节点和所有 Q-DDCA 局部观测。
2. 构造两个只在远端瓶颈位置上不同的网络状态。
3. 检查 Q-DDCA 在两种状态下是否选择相同下一跳。
4. 通过成对仿真或小规模精确搜索判断两种状态的最优下一跳是否相反。

若无法构造这种状态对，或 Q-DDCA 的额外损失在修复代码后消失，则核心假设被否定，停止该方向。

## 后续方法的准入条件

只有在上述失效成立后，才能设计修复方法。修复应优先寻找解除状态混叠所需的**最小额外信息**，例如有限范围的下游可靠性摘要，而不是直接提供完整全局状态或默认采用 GNN-RL。

## 不能声称

- Q-DDCA 是单跳路由。
- 首次研究多跳量子路由。
- 任意随跳数下降的曲线都证明 Q-DDCA 失效。
- 使用 GNN/RL 本身构成创新。
- 当前 reroute 在五跳后归零是算法机制结论；它可能是实现问题。

## 当前状态

**候选 idea，尚未完成立项验证。**

下一步唯一优先事项：修复并验证 Q-DDCA 高跳实现，然后执行局部状态混叠的最小反例实验。
